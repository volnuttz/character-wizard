interface:
  display_name: "Release character-wizard"
  short_description: "Prepare, verify, tag, and audit releases"
  default_prompt: "Use $release-pc-wizard to prepare and publish the next character-wizard release safely."
